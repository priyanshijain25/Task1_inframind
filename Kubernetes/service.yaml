apiVersion: v1
kind: Service

metadata:
  name: employee-api-service
  namespace: employee-api

spec:
  selector:
    app: employee-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: NodePort